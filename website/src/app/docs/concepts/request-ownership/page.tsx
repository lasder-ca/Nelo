import type { Metadata } from "next"
import { DocsShell } from "@/components/site/docs-shell"
export const metadata: Metadata = { title: "Request ownership" }
export default function Page() { return <DocsShell eyebrow="Core concept" title="Every piece of work has an owner." description="Request ownership is a structural rule: work created for a request must settle or clean up with that request."><h2>Handler scope</h2><p>The handler scope owns tasks and resources needed while route code is running. Use <code>context.fork()</code> for child work and <code>context.use()</code> for acquired resources.</p><h2>Delivery scope</h2><p>Returning a Response does not mean its body has finished. The delivery scope owns producers, file handles, sockets, and cleanup needed until the body completes or disconnects.</p><pre><code>{`context.delivery.use(() => producer.close())\nreturn new Response(producer.stream())`}</code></pre><h2>Cancellation</h2><p>The first typed cancellation reason wins and propagates through the ownership tree. Later failures do not erase the original cause.</p><h2>Cleanup order</h2><p>Resources are released once in reverse acquisition order. This keeps dependency teardown predictable on success, failure, disconnect, and shutdown.</p></DocsShell> }
